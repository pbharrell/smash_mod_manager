from folder import Directory
from file import File


def file_manager(file_1: str, path_1: str, file_2: str, path_2: str, file_structure:list):
    pass
    while True:
        user = input("Select an action:\n(1) for " + file_1 + " at " + path_1 + "\n(2) for " + file_2 + " at " + path_2)

        # if "1" in user:
        #
        # elif "2" in user:
        #
        # else:
        #     print("Please enter valid input!")